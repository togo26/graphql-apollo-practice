{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna clean && lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "dev": "lerna run dev --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
